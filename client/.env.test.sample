# Postgres credentials for the e2e webServer (Playwright).
# Copy this file to .env.test and adjust for your local environment.
# Database name and schema are replaced per run with a unique seed,
# so only host / port / user / password matter for isolation from unit tests.
DATABASE_URL=postgresql://test:test@localhost:5432/llm_chat_e2e_test
