{
  "name": "tenjo",
  "version": "0.2.0",
  "workspaces": [
    "client",
    "server",
    "chat-engine"
  ],
  "scripts": {
    "dev": "node scripts/dev.js",
    "lint": "npm -w client run lint && npm -w server run lint && npm -w chat-engine run lint",
    "format": "npm -w client run format && npm -w server run format && npm -w chat-engine run format",
    "build": "node scripts/build.js",
    "start": "npm start --prefix server",
    "migrate": "npm run migrate --prefix server",
    "setup": "npm ci && npm -w chat-engine run setup:browser-deps",
    "audit": "npm audit --workspaces --include-workspace-root",
    "license-report": "node scripts/generate-license-report.mjs"
  },
  "devDependencies": {
    "cross-spawn": "^7.0.6",
    "esbuild": "^0.28.1",
    "eslint": "^10.8.0",
    "license-checker-rseidelsohn": "^5.0.1"
  }
}
